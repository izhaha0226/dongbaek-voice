# 동백 직접 의존성. 나머지는 이들이 끌고 온다.
#
# ⚠ Apple Silicon 전용이다 — mlx 는 인텔 맥에서 돌지 않는다.
# 아래 하한은 실제로 돌려본 버전이다. 더 낮은 버전은 확인하지 않았다.

# 받아쓰기 (Apple Silicon GPU)
mlx-whisper>=0.4.3

# 마이크·스피커
sounddevice>=0.5.5
soundfile>=0.14.0
numpy>=2.4

# 화자 인증 (ECAPA 임베딩)
speechbrain>=1.1.0
torch>=2.13.0
torchaudio>=2.11.0

# 신경망 TTS. 실패하면 macOS `say` 로 자동 강등되므로 없어도 돈다.
supertonic>=1.3.1

# Claude 브릿지. config.BRIDGE = "sdk" 일 때만 쓰지만, 기본 설치에 넣어둔다 —
# 없는 상태로 스위치를 켜면 첫 명령에서야 알게 된다.
#
# ⚠ 이 패키지는 mcp<2.0.0 을 강제한다. mail_mcp.py 는 mcp 2.x 전용이라
#   한 venv 에 공존이 불가능하다 (같이 적으면 설치 자체가 해석 불가로 깨진다).
#   그래서 메일 MCP 는 requirements-mcp.txt 로 .venv-mcp 에 따로 깐다 —
#   setup.sh ①-2 가 한다.
claude-agent-sdk>=0.2.134

# macOS 캘린더(EventKit)·Mail.app 접근
pyobjc-framework-EventKit>=12.2
pyobjc-framework-Cocoa>=12.2
